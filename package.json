{
  "name": "phonegap-geo",
  "version": "0.0.1",

"description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "deepak",
  "config": {
    "unsafe-perm":true
  },
  "devDependencies": {
    "bower": "^1.3.1"
  },
  "scripts": {    
    "postinstall": "bower install"
  }

}
